namespace Client
{
    internal enum MessageTypes
    {
        DENY,
        LOGIN,
        LOGOUT,
        ACCEPT,
        ADD_USER,
        REMOVE_USER,
        SEND,
        RECEIVE,
        EXIT
    }
}
