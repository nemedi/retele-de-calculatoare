# Listen on all interfaces
agentAddress udp:161

# SNMP v2c community
rocommunity public

# System info (visible in Zabbix)
sysLocation "Lab Environment"
sysContact "teacher@example.com"

# Allow full OID tree
view all included .1

# Access control
access notConfigGroup "" any noauth exact all none none